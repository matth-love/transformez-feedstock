schema_version: 1

context:
  name: transformez
  version: "0.5.2"
  python_min: "3.12"

package:
  name: ${{ name | lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 63b37e42ce6a5499e20d824dac2865d7fdb482de75e67c12f41346ed6f777a19

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - transformez = transformez.cli:transformez_cli

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling >=1.5.0
    - hatch-vcs
    - wheel
    - pip
  run:
    - python >=${{ python_min }}
    - fetchez >=0.6.0
    - click
    - numpy >1.24
    - pyproj >3.6.1
    - rasterio >1.4.0
    - pyogrio >=0.7.0,
    - shapely >=2.0.0,
    - scipy

tests:
  - python:
      python_version:
        - ${{ python_min }}.*
        - "*"
      pip_check: true
      imports:
        - transformez
  - script:
      - transformez --help
    requirements:
      run:
        - python ${{ python_min }}.*

about:
  homepage: https://github.com/continuous-dems/transformez
  summary: A standalone utility for vertical elevation datum transformations.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - matth-love
    - mfisher87
